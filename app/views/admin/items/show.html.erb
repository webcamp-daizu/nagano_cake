<h3>商品詳細</h3>

<div><%= attachment_image_tag @item, :image, :fill, 300, 250 %></div>

<div>商品名</div>
<div><%= @item.name %></div>

<div>商品説明</div>
<div><%= @item.description %></div>

<div>ジャンル</div>
<div><%= @item.genre.name %></div>

<div>税込価格<br>（税抜価格）</div>
<div><%= @item.add_tax_included_price.to_s(:delimited) %>（<%= @item.tax_encluded_price.to_s(:delimited)%>）円</div>

<div>販売ステータス</div>
<div>
  <% if @item.is_active? %>
    <div class="text-success">販売中</div>
  <% else %>
    <div class="text-secondary">販売停止中</div>
  <% end %>
</div>

<div><%= link_to "編集する", edit_admin_item_path(@item) %></div>
