<!--ジャンル検索は後ほど実装-->

<h3>商品一覧</h3>
<div>（全<%= @items.total_count %>件）</div>

<% @items.each do |item| %>
  <div>
    <%= link_to item_path(item) do %>
      <%= attachment_image_tag item, :image, :fill, 200, 150 %>
    <% end %>
  </div>
  <div><%= item.name %></div>
  <div>¥<%= item.add_tax_included_price.to_s(:delimited) %></div>
<% end %>

<%= paginate @items %>